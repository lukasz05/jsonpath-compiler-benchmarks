extern "C" {
    fn free_result_buffer(result_buf: *mut u8);

    fn ondemand_eager_filters_canada_second_coord_component_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_canada_coord_476_1446_1_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_citm_seat_category_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_ast_nested_inner_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_ast_deepest_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_bestbuy_all_nodes_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_google_map_routes_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_google_map_travel_modes_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_inner_array_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_user_second_mention_index_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_retweet_count_58_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_retweet_count_gt_58_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_retweet_count_gte_1_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_twitter_text_abc_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_twitter_text_exists_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_twitter_consecutive_descendant_filter_segments_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_status_with_id_screen_name_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_canada_multiple_subqueries_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_canada_consecutive_filter_segments_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_canada_interleaved_filter_segments_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim1_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim2_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim3_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim4_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim5_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim6_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim7_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim8_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim9_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim10_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim11_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim12_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim13_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim14_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim15_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim16_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim17_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim18_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim19_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

    fn ondemand_eager_filters_claim20_binding(padded_input: *const u8, input_length: usize, result_length: *mut usize) -> *mut u8;

}


pub fn canada_second_coord_component(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_canada_second_coord_component_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn canada_coord_476_1446_1(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_canada_coord_476_1446_1_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn citm_seat_category(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_citm_seat_category_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn ast_nested_inner(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_ast_nested_inner_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn ast_deepest(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_ast_deepest_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn bestbuy_all_nodes(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_bestbuy_all_nodes_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn google_map_routes(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_google_map_routes_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn google_map_travel_modes(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_google_map_travel_modes_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn inner_array(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_inner_array_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn user_second_mention_index(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_user_second_mention_index_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn retweet_count_58(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_retweet_count_58_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn retweet_count_gt_58(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_retweet_count_gt_58_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn retweet_count_gte_1(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_retweet_count_gte_1_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn twitter_text_abc(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_twitter_text_abc_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn twitter_text_exists(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_twitter_text_exists_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn twitter_consecutive_descendant_filter_segments(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_twitter_consecutive_descendant_filter_segments_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn status_with_id_screen_name(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_status_with_id_screen_name_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn canada_multiple_subqueries(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_canada_multiple_subqueries_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn canada_consecutive_filter_segments(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_canada_consecutive_filter_segments_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn canada_interleaved_filter_segments(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_canada_interleaved_filter_segments_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim1(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim1_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim2(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim2_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim3(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim3_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim4(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim4_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim5(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim5_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim6(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim6_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim7(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim7_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim8(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim8_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim9(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim9_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim10(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim10_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim11(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim11_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim12(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim12_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim13(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim13_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim14(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim14_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim15(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim15_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim16(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim16_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim17(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim17_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim18(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim18_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim19(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim19_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}

pub fn claim20(padded_input: &[u8]) -> String {
    let input_ptr = padded_input.as_ptr();
    let mut result_length: usize = 0;
    unsafe {
        let result_ptr: *mut u8 = ondemand_eager_filters_claim20_binding(input_ptr, padded_input.len(), &mut result_length);
        let result_slice = std::slice::from_raw_parts(result_ptr, result_length);
        let result_str = String::from_utf8_unchecked(result_slice.to_vec());
        free_result_buffer(result_ptr);
        result_str
    }
}
